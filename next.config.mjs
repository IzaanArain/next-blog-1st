/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "images.pexels.com",
            "plus.unsplash.com",
            "picsum.photos"
        ]
    }
};

export default nextConfig;
